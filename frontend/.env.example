# Public API base URL for the Django backend API
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000/api

# Internal backend URL (used by Next.js server-side rewrites if deployed in container)
# BACKEND_INTERNAL_URL=http://backend:8000
